{
  "name": "openshift-pod-proxy",
  "version": "0.0.1",
  "description": "A reverse proxy to direct traffic to pods using an Oauth token secured API",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "OpenShift",
    "proxy"
  ],
  "author": "Brian Leathem",
  "license": "ISC",
  "dependencies": {
    "http-proxy": "^1.11.1"
  }
}
